package com.axonect.aee.template.baseapp.domain.enums;

/**
 * Event types for accounting CDR (Call Detail Record) events.
 */
public enum CdrEventType {
    ACCOUNTING_START,
    ACCOUNTING_INTERIM,
    ACCOUNTING_STOP
}
